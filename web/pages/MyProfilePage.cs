using OpenQA.Selenium.Remote;
using SQAT.web.core;

namespace SQAT_demo.web.pages
{
    public class MyProfilePage : AbstractPage
    {
        public MyProfilePage(RemoteWebDriver driver) : base(driver)
        {
        }
    }
}
